// Stub file — external class sync removed. Kept for import compatibility.

function setRawToken() {}
function setupFormbarSocket() {}
function getCurrentClassroom() { return null; }
function getCurrentClassId() { return null; }
function requestAndWaitForClassId() { return Promise.resolve(null); }

module.exports = { setupFormbarSocket, setRawToken, getCurrentClassroom, getCurrentClassId, requestAndWaitForClassId };
